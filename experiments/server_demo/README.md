# server_demo

Mini servidor Flask para pruebas dentro del TFG.

## Cómo ejecutar

```bash
cd server_demo
python -m venv .venv
.\.venv\Scripts\activate     # Windows
# source .venv/bin/activate  # Linux/Mac

pip install -r requirements.txt
python run.py
