\capitulo{3}{Conceptos teóricos}

Este capítulo introduce los conceptos teóricos necesarios para comprender la base técnica del trabajo. Se presentan fundamentos de inteligencia artificial, minería de datos, redes neuronales, procesamiento de imágenes y visión por computador. También se explican tareas habituales en el análisis automático de imágenes, como la clasificación, la detección de objetos, la estimación de puntos clave y la segmentación.

\section{Inteligencia artificial, minería de datos y aprendizaje automático}

La inteligencia artificial es el área de la informática que estudia la construcción de sistemas capaces de realizar tareas asociadas a la percepción, el razonamiento, el aprendizaje o la toma de decisiones \cite{russell2021artificial}. Dentro de este campo, el aprendizaje automático se centra en modelos que aprenden patrones a partir de datos, sin depender únicamente de reglas programadas de forma explícita \cite{bishop2006pattern}.

La minería de datos busca descubrir información útil en conjuntos de datos. Incluye tareas como clasificación, agrupación, detección de anomalías y extracción de patrones frecuentes \cite{han2011data}. En el análisis de imágenes, estas técnicas permiten transformar datos visuales en información interpretable.

La clasificación es una de las tareas más habituales. Consiste en asignar una entrada a una categoría o clase. En imágenes, puede aplicarse a la imagen completa o a regiones concretas, siempre que exista un conjunto de ejemplos etiquetados para entrenar el modelo \cite{witten2016data}.

El aprendizaje automático puede ser supervisado, no supervisado o por refuerzo. En el aprendizaje supervisado se aprende a partir de ejemplos etiquetados. En el no supervisado se buscan estructuras internas sin etiquetas previas. En el aprendizaje por refuerzo, un agente aprende mediante recompensas o penalizaciones obtenidas al interactuar con un entorno \cite{russell2021artificial}.

\section{Redes neuronales artificiales}

Las redes neuronales artificiales son modelos computacionales inspirados, en el funcionamiento de las redes de neuronas del cerebro, de ahí su nombre. Su objetivo es intentar emular ciertos procesos de transmisión, combinación y transformación de información mediante capas de unidades conectadas entre sí. Cada conexión tiene un peso asociado, y cada unidad transforma sus entradas para generar una salida. La combinación de muchas unidades permite aproximar funciones complejas \cite{goodfellow2016deep}.

Una red neuronal suele organizarse en una capa de entrada, una o varias capas ocultas y una capa de salida. La Figura~\ref{fig:red_neuronal_artificial} muestra una representación simplificada de esta estructura, formada por nodos conectados entre capas.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.65\textwidth]{figuras/red_neuronal_artificial.png}
	\caption[Ejemplo de red neuronal artificial.]{Ejemplo de red neuronal artificial con una capa oculta. Fuente: \href{https://commons.wikimedia.org/wiki/File:Artificial_neural_network.svg}{Cburnett, Wikimedia Commons}.}
	\label{fig:red_neuronal_artificial}
\end{figure}

Durante el entrenamiento, la red ajusta sus pesos para reducir el error entre la salida estimada y la salida esperada. Este error se mide mediante una función de pérdida, y uno de los mecanismos más utilizados para actualizar los pesos es la \textit{backpropagation} del error \cite{goodfellow2016deep}.

Una vez entrenada, la red puede utilizarse en fase de inferencia. En esta fase, los pesos no se modifican: el modelo aplica lo aprendido a nuevas entradas. En visión por computador, la entrada suele ser una imagen representada numéricamente, y la salida puede ser una clase, una probabilidad, una caja delimitadora, un conjunto de puntos o una máscara de segmentación.

\subsection{Redes neuronales convolucionales}

Las redes neuronales convolucionales son redes especialmente adecuadas para trabajar con imágenes. En este tipo de redes, las capas convolucionales aplican operaciones de convolución mediante filtros sobre regiones locales de la imagen, con el objetivo de extraer características visuales relevantes \cite{lecun1998gradient}.

En las primeras capas se suelen aprender patrones simples, como bordes, texturas o cambios de intensidad. En capas posteriores, esos patrones se combinan para representar formas y estructuras más complejas \cite{goodfellow2016deep}. La Figura~\ref{fig:red_convolucional} ilustra de forma esquemática esta transformación progresiva.

En cada unidad de la red se calcula una combinación ponderada de las salidas procedentes de la capa anterior, junto con un término de sesgo o \textit{bias}. Sobre ese valor se aplica una función de activación, que determina la salida de la neurona. Esta función introduce no linealidad en el modelo, permitiendo que la red aprenda relaciones más complejas que una simple transformación lineal \cite{goodfellow2016deep}.


\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.85\textwidth]{figuras/red_convolucional.png}
	\caption[Ejemplo de capas convolucionales.]{Representación simplificada de capas convolucionales en una red neuronal. Fuente: \href{https://commons.wikimedia.org/wiki/File:Convolutional_Layers_of_a_Convolutional_Neural_Network.svg}{Renanar2, Wikimedia Commons}.}
	\label{fig:red_convolucional}
\end{figure}

\subsubsection{Capas habituales en redes convolucionales}

Las redes convolucionales combinan distintos tipos de capas. Las más habituales son las siguientes:

\begin{itemize}
	\item \textbf{Capa convolucional.} Aplica filtros sobre la imagen o sobre mapas de características intermedios. Cada filtro aprende a detectar un patrón visual concreto \cite{lecun1998gradient}.
	
	\item \textbf{Capa de agrupación.} Reduce la resolución espacial de los mapas de características, disminuyendo el coste computacional y conservando la información más relevante \cite{goodfellow2016deep}.
	
	\item \textbf{Capa totalmente conectada.} Relaciona las salidas de una capa con las unidades de la siguiente. Suele emplearse en fases finales de clasificación.
	
	\item \textbf{Capa de normalización.} Estabiliza las activaciones durante el entrenamiento y puede acelerar la convergencia del modelo \cite{ioffe2015batch}.
\end{itemize}

Estas capas permiten construir modelos capaces de extraer información visual de forma progresiva. Por ello, las redes convolucionales son una base habitual para clasificación de imágenes, detección de objetos, estimación de puntos clave y segmentación.

\section{Procesamiento de imágenes y visión por computador}

Una imagen digital puede representarse como una matriz de píxeles. Cada píxel contiene valores numéricos que describen intensidad o color. En imágenes en escala de grises se utiliza un único canal, mientras que en imágenes a color se emplean varios canales \cite{gonzalez2018digital}.

El procesamiento de imágenes agrupa técnicas orientadas a modificar o preparar una imagen. Incluye operaciones como redimensionado, conversión de color, filtrado, normalización, reducción de ruido o transformación geométrica \cite{gonzalez2018digital}.

La visión por computador busca extraer información útil a partir de imágenes o secuencias de vídeo. Para ello combina procesamiento de imágenes, aprendizaje automático y modelos matemáticos capaces de interpretar contenido visual \cite{szeliski2022computer}.

Entre sus tareas más habituales se encuentran la clasificación de imágenes, la detección de objetos, la estimación de puntos clave, la segmentación y el seguimiento de elementos en vídeo. Cada una produce un tipo de salida diferente y permite analizar la imagen con distinto nivel de detalle.

\subsection{Clasificación de imágenes}

La clasificación de imágenes consiste en asignar una o varias etiquetas a una imagen completa. El modelo recibe la imagen como entrada y devuelve una clase o una distribución de probabilidades entre varias clases posibles\cite{bishop2006pattern}.

Una \textit{bounding box} representa la región rectangular en la que el modelo estima que se encuentra un objeto. Normalmente se define mediante las coordenadas de sus esquinas o mediante una posición inicial junto con su anchura y altura. Esta representación permite indicar no solo qué objeto se ha detectado, sino también su localización aproximada dentro de la imagen.

Esta tarea es útil cuando interesa conocer la categoría global de una imagen. Sin embargo, no proporciona información precisa sobre la posición de los elementos que aparecen en ella.

\subsection{Detección de objetos}

La detección de objetos consiste en localizar entidades dentro de una imagen y asignarles una clase. Su salida habitual incluye una caja delimitadora, también denominada \textit{bounding box}, una etiqueta y una puntuación de confianza \cite{szeliski2022computer}.

La Figura~\ref{fig:deteccion_objetos} muestra un ejemplo de detección mediante cajas delimitadoras, donde cada objeto se representa con una región rectangular asociada.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.60\textwidth]{figuras/deteccion_objetos.png}
	\caption[Ejemplo de detección de objetos.]{Ejemplo de detección de objetos mediante cajas delimitadoras. Fuente: \href{https://commons.wikimedia.org/wiki/File:ObjectDetectionExample.png}{Hughesperreault, Wikimedia Commons}.}
	\label{fig:deteccion_objetos}
\end{figure}

A diferencia de la clasificación, la detección no solo indica qué aparece en la imagen, sino también dónde se encuentra. Por ello, resulta adecuada cuando una escena puede contener varios elementos de interés.

\subsection{Estimación de puntos clave}

La estimación de puntos clave, también denominados \textit{landmarks}, consiste en localizar posiciones relevantes dentro de una estructura. En lugar de delimitar un objeto completo mediante una caja, identifica puntos concretos, como articulaciones, extremos o referencias anatómicas \cite{szeliski2022computer}.

Este enfoque permite representar estructuras con mayor detalle. En una mano, los puntos clave pueden corresponder a nudillos y extremos de los dedos. En una pose corporal, pueden representar hombros, codos, muñecas, caderas, rodillas o tobillos.

La Tabla~\ref{tabla:deteccion-puntos-clave} recoge las diferencias principales entre la detección de objetos y la estimación de puntos clave.

\begin{table}[htbp]
	\centering
	\small
	\begin{tabular}{p{0.25\textwidth} p{0.31\textwidth} p{0.31\textwidth}}
		\toprule
		\textbf{Aspecto} & \textbf{Detección de objetos} & \textbf{Estimación de puntos clave} \\
		\midrule
		Finalidad & Localizar y clasificar entidades visibles en una imagen. & Localizar puntos relevantes de una estructura concreta. \\
		\midrule
		Salida habitual & Caja delimitadora, clase y confianza. & Coordenadas de puntos clave y posibles conexiones entre ellos. \\
		\midrule
		\shortstack[l]{Representación\\visual} & Rectángulos y etiquetas sobre la imagen. & Puntos, esqueleto o malla sobre la imagen. \\
		\midrule
		Nivel de detalle & Describe la posición global del objeto. & Describe la estructura interna del elemento analizado. \\
		\bottomrule
	\end{tabular}
	\caption[Comparación entre detección de objetos y puntos clave.]{Comparación entre detección de objetos y estimación de puntos clave.}
	\label{tabla:deteccion-puntos-clave}
\end{table}

\subsection{Segmentación de imágenes}

La segmentación de imágenes consiste en dividir una imagen en regiones significativas. A diferencia de la detección de objetos, que utiliza cajas delimitadoras, la segmentación trabaja a nivel de píxel. Cada píxel puede asignarse a una región, una clase o una instancia concreta \cite{gonzalez2018digital,szeliski2022computer}.

Existen distintos tipos de segmentación. La segmentación semántica asigna una clase a cada píxel sin distinguir objetos diferentes de la misma clase. La segmentación de instancias diferencia cada objeto individual. La segmentación panóptica combina ambas aproximaciones \cite{long2015fully,kirillov2019panoptic}.

La Figura~\ref{fig:segmentacion_imagen} muestra un ejemplo visual en el que la imagen queda dividida en varias zonas diferenciadas.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.60\textwidth]{figuras/segmentacion_imagen.png}
	\caption[Ejemplo de segmentación de imágenes.]{Ejemplo de segmentación de imágenes a nivel de regiones. Fuente: \href{https://commons.wikimedia.org/wiki/File:Image-segmentation-example-segmented.png}{Martin Thoma, Wikimedia Commons}.}
	\label{fig:segmentacion_imagen}
\end{figure}

La segmentación proporciona una descripción más precisa que la detección mediante cajas, ya que permite delimitar la forma real de los elementos. Sin embargo, suele requerir modelos más específicos y conjuntos de datos anotados con mayor detalle.
\clearpage
\section{Arquitecturas de detección de objetos en una etapa}

Las arquitecturas de detección de objetos pueden clasificarse, de forma general, en detectores de dos etapas y detectores de una etapa. Los detectores de dos etapas generan primero regiones candidatas y después las clasifican. Los detectores de una etapa realizan la localización y la clasificación de forma directa \cite{redmon2016yolo}.

Su planteamiento original formula la detección como un único problema de regresión, en el que una red predice cajas delimitadoras y probabilidades de clase en una sola evaluación de la imagen \cite{redmon2016yolo}.

De forma simplificada, una arquitectura de detección en una etapa incluye tres bloques: extracción de características visuales, combinación de información a distintas escalas y generación de predicciones finales. Estas predicciones contienen coordenadas de cajas, puntuaciones de confianza y clases asociadas.

Tras obtener las predicciones, se aplica un posprocesado para eliminar detecciones redundantes o poco fiables. Una técnica habitual es la supresión de máximos no máximos, que conserva la caja más representativa cuando varias predicciones se solapan sobre el mismo objeto \cite{szeliski2022computer}.

Este tipo de arquitectura permite resolver detección y clasificación en una misma fase de inferencia. Por ello, es una aproximación habitual en aplicaciones que requieren detección visual eficiente.