\capitulo{1}{Introducción}

En los últimos años, la inteligencia artificial aplicada a la visión por computador ha experimentado un importante crecimiento en tareas como la detección de objetos, la estimación de pose, el reconocimiento de manos o el análisis automático de imágenes. Estas técnicas permiten extraer información útil de una imagen y devolver resultados interpretables, tanto de forma visual como mediante datos estructurados.

Sin embargo, el uso práctico de estos modelos no se limita a ejecutarlos de forma aislada desde un \textit{script}. Para que puedan ser utilizados dentro de una aplicación real, es necesario diseñar un sistema capaz de recibir imágenes, gestionar usuarios y permisos, seleccionar el análisis correspondiente, ejecutar el procesamiento en el servidor y mostrar los resultados de forma clara.

El presente Trabajo de Fin de Grado aborda el diseño e implementación de una plataforma \textit{web} extensible para el procesamiento de imágenes mediante \textit{pipelines} de inteligencia artificial. La finalidad principal no es solo integrar modelos como YOLO o MediaPipe, sino construir una arquitectura modular que permita añadir nuevos modelos, modos de ejecución y flujos de análisis sin modificar de forma significativa el funcionamiento general del sistema.

La aplicación se ha desarrollado con Flask y se organiza en controladores, servicios, modelos de datos y vistas. Esta separación facilita el mantenimiento del código y permite desacoplar la interfaz, la persistencia y la ejecución de los modelos. Además, la ejecución de los \textit{pipelines} se apoya en el patrón factoría, que permite resolver dinámicamente qué modelo y qué modo debe ejecutarse en cada etapa.

Desde el punto de vista funcional, el sistema permite el registro e inicio de sesión de usuarios, la consulta de \textit{pipelines} disponibles, la subida de imágenes, la ejecución del análisis, la visualización de resultados y la descarga temporal de los archivos generados. También incorpora gestión de planes, alquiler temporal de modelos, historial de ejecuciones y una zona de administración para consultar usuarios, revisar ejecuciones y gestionar \textit{pipelines}.

Finalmente, el proyecto se ha preparado para facilitar su despliegue y validación técnica. La aplicación puede ejecutarse mediante Docker, favoreciendo la reproducibilidad del entorno, y se han desarrollado pruebas unitarias sobre la parte del \textit{backend}, alcanzando una cobertura final del 92,9 \%.

\section{Materiales adjuntos}

Junto a esta memoria se entregan los siguientes materiales:

\begin{itemize}
	\item Anexos técnicos del proyecto.
	\item Código fuente completo de la aplicación \textit{web}.
	\item Ficheros de configuración utilizados por los modos de procesamiento.
	\item \textit{Script} de inicialización de datos para preparar un entorno de demostración.
	\item Ficheros de despliegue y contenerización mediante Docker.
	\item Ficheros de configuración para el análisis estático del código con SonarCloud.
	\item \textit{Tests} unitarios automáticos del \textit{backend}.
	\item Vídeo de funcionamiento de la aplicación.
	\item Repositorio del proyecto disponible a través de internet \footnote{\url{https://github.com/alvaroooper/TFG.git}{ Repositorio del proyecto}}.
	\item Espacio de planificación y seguimiento del proyecto en Jira\footnote{\url{https://alvaro-tfg.atlassian.net/}{ Espacio de planificación del proyecto}}.
	

\end{itemize}

